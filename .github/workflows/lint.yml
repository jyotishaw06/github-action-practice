# what is the goal of xthe workflow
# Goal: Whenever someone changes the python code
# the liner will check if the sytax , format, indentation(Linting) is correct or not
name: Auto Linter

on:
  push:
    paths: "*.py"

jobs:
  code:
    runs-on: ubuntu-latest
    #this this step will automatically clone the code of the current branch into the runer
    steps:
      - name: Checkout/Clone the code
        uses: actions/checkout@v7
    
        # this step install the lining tool

        # 2. Install and set up the Python environment
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.14' 
          
      - name: install Linter
        run: pip install ruff
        # this step will run the linter
      - name: Check the file for linting
        run: ruff check .


